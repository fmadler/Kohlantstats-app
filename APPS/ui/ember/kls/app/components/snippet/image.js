import Component from '@glimmer/component';
import { action } from '@ember/object';

export default class SnippetImageComponent extends Component {
    
    @action
    getDefault(scope) {
        debugger;
    }
}
